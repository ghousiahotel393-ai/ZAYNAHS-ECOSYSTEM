name: cctv
description: CCTV Monitoring, Segmented Recording Pipeline, Timeline Playback, and Sacred Retention for Zaynahs Ecosystem.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  core:
    path: ../core
  crypto: ^3.0.3
  database:
    path: ../database
  storage:
    path: ../storage
  sqlite3: ^2.9.4

dev_dependencies:
  test: ^1.24.0
