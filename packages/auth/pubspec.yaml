name: auth
description: Operator identity, RBAC permission resolver, device trust lifecycle, and pairing protocol for Zaynahs Ecosystem.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  meta: ^1.11.0
  crypto: ^3.0.3
  core:
    path: ../core
  database:
    path: ../database

dev_dependencies:
  test: ^1.24.0
