# PHASE 05 — SYNC EVENT MODEL & SCHEMA

## EVENT SCHEMA
`event_id` (UUIDv7), `event_type`, `aggregate_id`, `aggregate_type`, `device_id`, `user_id`, `created_at`, `logical_version`, `payload` (JSON), `hash` (SHA-256), `signature`, `schema_version`, `state`.
