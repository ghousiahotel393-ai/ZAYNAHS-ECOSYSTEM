name: zaynahs_ecosystem
description: Production-grade Zaynahs Ecosystem — Local-First Peer-to-Peer POS & Collaboration.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  core:
    path: packages/core
  ui:
    path: packages/ui
  database:
    path: packages/database
  auth:
    path: packages/auth
  storage:
    path: packages/storage
  sync:
    path: packages/sync
  network:
    path: packages/network
  sqlite3: ^2.9.4
  crypto: ^3.0.3

dev_dependencies:
  test: ^1.24.0
